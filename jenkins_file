pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo " hello galaxy 1234"
                sh 'echo using shell within Jenkinsfile'
                echo 'not using shell in the Jenkinsfileeee'
            }
        }
    }
}
